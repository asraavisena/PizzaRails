<%= form_with(model: order, local: true) do |form| %>
  <% if order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
      <% order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :orderID %>
    <%= form.number_field :orderID, id: :order_orderID %>
  </div>

  <div class="field">
    <%= form.label :customerID %>
    <%= form.number_field :customerID, id: :order_customerID %>
  </div>

  <div class="field">
    <%= form.label :orderID %>
    <%= form.number_field :orderID, id: :order_orderID %>
  </div>

  <div class="field">
    <%= form.label :total %>
    <%= form.number_field :total, id: :order_total %>
  </div>

  <div class="field">
    <%= form.label :delivered %>
    <%= form.time_select :delivered, id: :order_delivered %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
